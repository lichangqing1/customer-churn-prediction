-- Logical schema for the customer churn analytics warehouse.
-- The physical tables are generated by scripts/build_churn_warehouse.py.

-- Bronze layer
-- bronze_raw_customer_churn: raw Telco churn data as loaded from CSV.

-- Silver layer
-- silver_clean_customers: cleaned and feature-engineered customer-level data.

-- Dimensions
-- dim_customer(customer_id, gender, senior_citizen, partner, dependents)
-- dim_contract(contract_id, contract)
-- dim_service(service_id, internet_service, phone_service, online_security, tech_support)
-- dim_payment(payment_id, payment_method, paperless_billing)

-- Fact table
-- fact_customer_churn(
--   customer_id,
--   contract_id,
--   service_id,
--   payment_id,
--   tenure,
--   monthly_charges,
--   total_charges,
--   avg_charge_per_tenure,
--   high_monthly_charge,
--   short_tenure,
--   is_long_term_contract,
--   tenure_group,
--   monthly_charge_group,
--   churn_flag
-- )

-- Gold analytics tables
-- gold_churn_by_contract
-- gold_churn_by_payment
-- gold_high_risk_segments

-- Operational metadata
-- meta_pipeline_runs(run_id, status, source_rows, fact_rows, completed_at_utc)
-- meta_data_quality_results(check_name, passed, details, run_id, gate, checked_at_utc)
